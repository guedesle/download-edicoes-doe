name: Sync central skills mirror

on:
  workflow_dispatch:
  schedule:
    - cron: "17 * * * *"
  push:
    paths:
      - ".github/workflows/sync-central-skills.yml"

permissions:
  contents: write

jobs:
  sync:
    uses: guedesle/SKILLS/.github/workflows/mirror-consumer.yml@main
    permissions:
      contents: write
